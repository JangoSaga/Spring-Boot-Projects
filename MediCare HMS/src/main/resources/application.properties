spring.application.name=medicare

# Server Configuration
server.port=${PORT:8080}

# PostgreSQL Configuration (Use environment variables or defaults)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-broad-poetry-aqumxoef-pooler.c-8.us-east-1.aws.neon.tech/medicare_hms?sslmode=require&channelBinding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_A5hRm6yGidMZ}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false

# PostgreSQL Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security
spring.security.user.name=${SECURITY_USER_NAME:admin}
spring.security.user.password=${SECURITY_USER_PASSWORD:admin123}

# Logging
logging.level.root=WARN
logging.level.com.spring.medicare=INFO

# Actuator - Health checks for monitoring
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized